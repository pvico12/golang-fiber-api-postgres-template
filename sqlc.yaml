version: "2"
sql:
  - engine: "postgresql"
    queries: "db/queries.sql"
    schema: "db/schema.sql" # Optional, can be omitted if you don't need schema
    gen:
      go:
        package: "db"
        out: "db/sqlc"
